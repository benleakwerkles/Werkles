import type { ReactNode } from "react";

export const metadata = {
  title: "Sign in",
  description: "Sign in to your Werkles workshop."
};

export default function LoginLayout({ children }: { children: ReactNode }) {
  return children;
}
