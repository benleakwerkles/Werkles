import type { ReactNode } from "react";

export const metadata = {
  title: "Your Workshop",
  description: "Review and correct the working brief built from your Intake."
};

export default function WorkshopLayout({ children }: { children: ReactNode }) {
  return children;
}
