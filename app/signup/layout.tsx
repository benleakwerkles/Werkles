import type { ReactNode } from "react";

export const metadata = {
  title: "Start free",
  description: "Create a free Werkles account. Dues only when the floor earns it."
};

export default function SignupLayout({ children }: { children: ReactNode }) {
  return children;
}
