# TO CURSOR + ENDER/DOOZER@MEDULLINA — HARVEY MOBILE V/P/G PACKET v4

Date: 2026-07-16
Executor/push owner: Dink@Medullina (root)
Canon: `benleakwerkles/Werkles` / `codex/cloud-harvey-mobile-vpg-20260715` / `Harvey/Werkles Mobile/`
Pulled head: `e1f2836f0cc82a2658aaff5897a2a73aee71cda7`

## V — Fresh handoff

Cursor and Ender/Doozer independently pulled the exact cloud head. Root executes all writes. Thufir and Bean are not engaged because the two pulls converged without an unresolved review gap.

## P — Respective Aeye state

### Cursor@Medullina

Ranked: (1) untrusted manual receipt lab, (2) cloud invariant test gate, (3) app-shell session state, (4) evidence-first route truth.

### Ender/Doozer@Medullina

Ranked: (1) contract tests plus Android Expo export artifact, (2) local receipt proof lab, (3) investigate unrelated Vercel status, (4) repair stale docs and packet status.

Both verified:

- Head `e1f2836f...`; exact-head Harvey run `29475883015` successful.
- No test command, no contract tests, and no Metro/Expo export gate exist yet.
- Access hard-codes receiver input to `null` and re-reduces from initial state.
- Relay/status, actionable returns, and Thinkit probes timed out after three seconds. Live relay state remains unproven.
- Committed June 28–29 Flock evidence remains historical only.

## G — Execute these two

### 1. Deterministic cloud contract tests

Add a pinned TypeScript test runner and deterministic, network-free tests for machine normalization/key titles, return-receipt allowlisting/correlation, Duck secret/size/depth rejection, Flock lifecycle forward/replay/backward/terminal behavior, and evidence-age calculations. Make `npm test` a required Actions step.

### 2. Android Expo export smoke artifact

Run an Android Expo/Metro export after lint, tests, and typecheck. Upload the result with bounded retention and label it clearly as a bundle smoke artifact, not an installable APK.

## Receipt contract

A single final SHA must pass install, lint, contract tests, typecheck, and Android export. Return the Actions run and retained artifact proof. No deployment, live dispatch, or device/key mutation.

Status: ROOT EXECUTION STARTED.
