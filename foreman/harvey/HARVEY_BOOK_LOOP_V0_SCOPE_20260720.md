# Harvey Book Loop V0 Scope

Status: `APPROVED__IN_PROGRESS`

Parent lane: `Harvey Project Flock Coordination Protocol`  
Environment: isolated Doss Harvey worktree plus private
`benleakwerkles/Harvey-Builder-Mailbox` on `main`  
Budget: no paid calls

## Allowed

- publish the already-completed Swanson Packet 002 return without unrelated files;
- define the role-neutral, bounded mailbox routing contract;
- prepare one Orson bootstrap packet and release manifest;
- implement a local filesystem-only, dry-run-by-default pointer router;
- validate canonical hashes, terminal return markers, exact packet targets,
  path containment, replay, locking, and compare-and-swap pointer state;
- atomically advance the private mailbox pointer for the approved bootstrap;
- commit and push only the reviewed Book Loop V0 boundary; and
- report one final manual `KNock` for the preserved Orson Work thread.

## Forbidden

- reading the whole book or inventing Packet 003;
- automatically submitting to the Orson browser tab;
- secrets, provider login, OAuth, billing, deployment, schema changes, or
  production-data mutation;
- treating pointer advancement, `SENT`, or artifact existence as delivery,
  incorporation, acceptance, effect, completion, outcome, or returned time;
- modifying the dirty canonical Werkles checkout; and
- staging or committing unrelated work.

## Limits

- one source return: Swanson Packet 002;
- one delivered destination: corrected Orson bootstrap Packet 001; Packet 000 is
  preserved as an undelivered, superseded red-team artifact;
- one authoritative pointer: `CURRENT_PACKET.md`;
- router writes only the pointer and its transition receipt;
- exact replay is a no-op; conflicting reuse fails closed.

## Stop condition

Stop at `AWAITING_ORSON_ROUTE_BINDING` after the reviewed code and mailbox
transition are pushed. Ben performs one final `KNock` in the existing Orson
ChatGPT Work thread. No permanent Orson direct route is claimed until that
thread returns a supported callable binding receipt.
