{
  "missionId": "WERKLES_IDENTITY_SPINE_COMPUTER_SOURCE_CORRECTION",
  "version": "v0.1",
  "issuedBy": "Heimerdinker@Betsy, Werkles Foreman",
  "slice": "Computer correction pass against exact local identity facts; return a bounded Doozer-ready source contract, not another generic architecture.",
  "sharedContext": [
    "Petra returned IDENTITY_SPINE_ACCEPTANCE_READY: durable means canonical identity plus refresh/new-tab/restart/logout-login/fresh-session recovery, exact Intake/Workshop/Profile/Recommendation continuity, explicit browser-vs-account truth, and no authenticated demo fallback.",
    "Exact local config fact: .env.local contains no NEXT_PUBLIC_SUPABASE_URL and no NEXT_PUBLIC_SUPABASE_ANON_KEY names. Therefore hasSupabaseBrowserConfig() is false on the current localhost.",
    "Exact source fact: lib/client-auth.ts returns dev-preview-token when local preview is unlocked and a dev preview session exists.",
    "Exact source fact: components/squibb/account-aware-recommendation-surface.tsx treats null and dev-preview-token alike and returns the server-rendered initialSession instead of calling /api/bellows/recommendations/current.",
    "Exact source fact: lib/squibb/bellows-owner-session.ts maps a local walkthrough session or dev-preview-token to member_dev-preview-user; dev-preview Intake POST and Recommendations therefore share one synthetic owner rather than a real account.",
    "Exact source fact: a prepared Tier-1 gate says the member_concierge_intakes migration/RLS and runtime configuration are not applied/verified. The migration and source adapters exist, but local account durability remains blocked.",
    "Observed consequence: a user who believes gimprobotester is signed in can receive old cookie-bound member_dev-preview-user bakery data, while the code-level continuity mocks still pass.",
    "No code/provider/secrets/env edit/SQL/schema/RLS/push/deploy/new task/subagent/environment is authorized."
  ],
  "liveStrings": [
    "Saved to your Werkles account.",
    "Saved in this browser only.",
    "Local walkthrough — not your member account.",
    "Sign in to save this to your Werkles account."
  ],
  "knownGaps": [
    "Current local UI can look signed in while only a shared synthetic preview owner exists.",
    "Existing green continuity tests prove source contracts, not real Supabase account persistence.",
    "The current source may still need a safe pre-gate correction that prevents preview identity from impersonating account identity and purges stale fixture fallback from authenticated-looking flows."
  ],
  "cousins": {
    "COMPUTER": {
      "vision": "Attack the exact preview-to-fake-owner chain and produce the smallest safe pre-gate correction that stops false account identity now while preserving the later real Supabase path.",
      "pull": "Treat the listed source facts as authoritative local observations. Distinguish immediate source-only truth repairs from the separate schema/RLS/runtime-config human gate.",
      "go": [
        "Return the exact failure chain and classify each component as canonical, preview-only, or incorrectly conflated.",
        "Return one minimal Doozer-ready patch contract with exact named files, behavior before/after, rollback boundary, and tests; it must not claim real account saving without Supabase config/schema proof.",
        "Return hostile cases for stale bakery state, two local tabs, logout/login, preview cookie residue, and a later real Supabase session replacing preview state."
      ],
      "momentum": "Return IDENTITY_SOURCE_CORRECTION_READY, NEEDS_MORE_BYTES, or BLOCKED plus the exact next Doozer packet seed.",
      "outOfLane": "No code mutation, provider call, credentials, env edit, SQL/schema/RLS, push, deploy, new task, subagent, or secondary model."
    }
  }
}
