# Formation Value — Bean Foreman Assimilation

Source: `foreman/handoffs/inbox/FROM_BEAN_WERKLE_FORMATION_VALUE_LOOP_v0.1.md`  
Custody: validated `OK`  
State: `FOREMAN_ASSIMILATED__PRACTICE_TRUST_PATCHED__REAL_LOOP_BLOCK_PRESERVED`

## Accepted and applied

1. Synthetic partner material must be described as a generated practice prompt,
   never as the other person's answer.
2. Actor switching must preserve separate choices/notes and visibly name the
   current practice actor.
3. Private predictions stay in session storage and must never enter the
   Formation local-storage record, shared floor, matching, or Personal Bellows.
4. Unilateral or stale acceptance stays out of the shared floor; changing exact
   wording invalidates both prior approvals.
5. Save-scope and consent copy must remain neutral, never an upgrade threat.

Applied proof:

- `scripts/foreman/werkle-formation-bean-hostile-browser-smoke.mjs`
- `scripts/foreman/werkle-formation-contract-smoke.ts`
- `components/werkle/partner-perspective-exercise.tsx`
- `components/werkle/formation-workbench.tsx`

## Patched before product canon

- Bean proposed an immutable `(memberId, exactText, acceptedAt, versionHash)`
  record. The current browser-local model proves participant-scoped choices,
  exact wording, revision number, and accepted revision. It does not prove real
  member identity, an immutable timestamp, or a cryptographic version hash. We
  will not invent those claims.
- Location is not categorically forbidden: a member-chosen location/radius is a
  legitimate matching input. Hidden location inference, precise movement data,
  and protected-category proxies remain forbidden.
- A private expectation about how much someone might contribute to a Werkle is
  not a public wealth rank. It must remain private/consent-scoped and cannot
  become net-worth, prestige, visibility, or match-score data.

## Block honored

The current surface remains a clearly labeled one-tester practice exercise.
Real two-member collaboration remains blocked on authenticated actor custody,
account persistence, invitation/permission boundaries, and post-build CBCC
proof. The practice surface passing local hostile tests does not clear that
production boundary.

## Still owed

- Lady Jessica/Doozer visual and source walk using Bean's checklist.
- Ender comprehension/emotional-safety return when its established route is
  available.
- Computer source review must be reissued only from locally verified contracts;
  its two truncated replies remain quarantined.

