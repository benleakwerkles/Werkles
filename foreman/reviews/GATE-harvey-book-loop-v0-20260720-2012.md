# Gate - Harvey Book Loop V0

Decision: `APPROVED`  
Confidence: `HIGH` for the bounded local router; `LOW` for a permanent Orson
direct route until the provider supplies a callable receiver.

## Operator approval

> APPROVE BOOK LOOP V0: publish Swanson receipt 002, add the bounded mailbox routing contract, and implement the receipt-to-next-packet router in Harvey. Preserve the existing Orson Work thread and prepare one final KNock bootstrap packet for it.

## Blast radius

- private Harvey Builder Mailbox routing artifacts and sole active pointer;
- isolated Harvey cloud-relay worktree scripts, tests, scope and review records;
- no canonical dirty Werkles files, deployment, provider, secret, schema,
  production data, or browser submission.

## Unknowns

- whether ChatGPT Work exposes a supported callable route to the existing Orson
  thread;
- whether Orson's connected GitHub surface can write the Packet 003 artifacts.

## Risks and controls

- stale/concurrent pointer: compare-and-swap plus cross-process lock;
- content substitution: `SHA256_LF_UTF8_V1` recomputation;
- path escape: normalized contained paths plus link rejection;
- false completion: claim-limited transition receipt and explicit UI/CLI truth;
- user mule labor: one final bootstrap `KNock`, then stop and measure the actual
  route result instead of promising automation.

## Current blocked boundary

`AWAITING_ORSON_ROUTE_BINDING` after the mailbox points to Orson. The V0 does
not authorize automatic submission into an arbitrary browser tab.

Red-team correction: mailbox routing contract
`MAILBOX_ROUTING_CONTRACT_V0_1_20260720.md` supersedes V0 for new transitions.
The final active bootstrap is Orson Packet 001; published Packet 000 is retained
as undelivered superseded history.
