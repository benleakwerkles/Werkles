# V — Actual CBCC review first must be machine state

Status: `VISION — LOCAL COCKPIT / SPEAKER / PACKETS ONLY`

Execution context: `CODEX_LOCAL` on BETSY, canonical repo, branch `maker/site-g-20260703`, commit `93b79d1`, dirty shared tree preserved, nothing staged.

## Vision

“Use the CBCC” cannot remain a chat-memory instruction or a paragraph an agent can read and then ignore. For every member-facing trust, matching, money, or major UX slice, the repo must expose one active state that answers:

1. which actual CBCC seats were asked;
2. which actual return receipts exist;
3. whether implementation may begin;
4. who is assigned to build after review;
5. what remains prohibited.

The Foreman does not implement while that state says `BLOCKED_PENDING_ACTUAL_CBCC_RETURNS`. Outgoing packets do not satisfy the condition. Old reviews may inform the packet, but do not count as current-slice participation.

## Operator cause

The Foreman built a dual-purpose Intake slice locally, then described it as built from CBCC reviews even though no actual cousin participated in the current cycle. Automated checks passed while the real Chrome view was visually broken and, after a production build rewrote `.next` beneath the running dev server, temporarily rendered as browser-default HTML. The Operator again had to catch both the solo-build pattern and the missing visual review.

## Bounded work

- draft a Speaker causal entry;
- add an active review-first slice state to the cockpit;
- prepare focused current-slice packets for Ender, Bean, Lady Jessica, and Doozer;
- record a VPGM receipt that distinguishes requests from returns.

## Hard edges

No implementation work on the Intake/Recommendations slice. No subagents, substitute environments, external-seat impersonation, push, deploy, schema, provider action, secret, production mutation, or claim that packets equal reviews.

## Stop condition

Stop after the active state, Speaker draft, focused packets, re-pull, and receipt exist. Implementation remains blocked until actual CBCC return receipts are present and synthesized.
