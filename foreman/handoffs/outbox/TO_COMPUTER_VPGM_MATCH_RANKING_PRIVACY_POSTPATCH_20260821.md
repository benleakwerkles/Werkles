# TO COMPUTER / THUFIR — Ranking/privacy post-patch seal

Return `PASS`, `PATCH`, or `BLOCK` on the two repairs made from your post-code
attack.

1. The member-facing explainer now says `#1 is the strongest current fit` and
   that later positions may favor meaningfully different kinds of help among
   people still strong enough.
2. The deterministic selector now emits `orderReason` with every candidate:
   strongest current fit; next score-ordered fit; or shown ahead of a near fit
   to add a meaningfully different contribution. The selected-profile panel
   displays that reason next to `Current order #N`.

The narrow versioned scorer input remains hand-enumerated and frozen. City and
state are copied explicitly; no ZIP/neighborhood/IP/timezone/locale field
exists. Token-overlap weights are symmetric by overlap count, not job title,
industry value, or wage proxy. All five focused contracts and TypeScript pass.

Review the bounded candidate. No implementation, provider, schema, push, or
deploy action.
