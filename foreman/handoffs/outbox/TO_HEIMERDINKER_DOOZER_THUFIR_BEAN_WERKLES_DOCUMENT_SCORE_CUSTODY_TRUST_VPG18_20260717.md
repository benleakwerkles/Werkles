# Flock Packet — Document Score Custody and Trust VPG18

Status: `CLAIMED`
Machine: `BETSY`
Execution/push owner: Heimerdinker / Dink@Betsy
Review seats: Doozer@Betsy, Thufir@Betsy, and Bean@Betsy
Repository: `benleakwerkles/Werkles`
Starting source: `99993964bc9f189b4baa02c32839ada03c93fe95`

## Mission

Keep pasted-document custody and score language truthful at every layer of the internal document-score experience.

## VPG PREPARE — read-only pull

Inspect the document signal source, recommendation conversion, activity-ledger condition, score contract, and visible score limitations. Return exactly two bounded corrections without changing scoring, gates, storage, flags, or delivery.

Heimerdinker owns all edits, verification, commits, pushes, localhost hands, and any future Preview hands.

## VPG GO — exactly two moves

1. Introduce explicit `operator_document` / `ephemeral_document` source contracts so pasted evidence says `Pasted document` and never behaves like a saved intake or activity ledger.
2. Replace eligibility/confidence implications with `not ruled out` / `rule-support band` language and always show that scores are not probability, accuracy, eligibility, verification, or outcome predictions.

## Acceptance

- Discovery and authenticated-profile sources retain their existing behavior.
- Pasted-document evidence never says `Your intake`; no intake activity ledger renders for the ephemeral document mode.
- No API or visible `eligible_count`, `Eligible`, or ambiguous `confidenceLabel` remains in document-score delivery.
- The score limitation renders even when every path is ruled out.
- No browser/cursor control, PR, manual deploy, Production action, persistence, LLM/provider call, member-data read, schema/RLS, or Tier B work.
