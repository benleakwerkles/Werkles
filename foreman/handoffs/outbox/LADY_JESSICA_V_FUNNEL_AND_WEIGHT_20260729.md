# V — Funnel room + page weight

Seat: **Lady Jessica (Cursor) — Werkles.com Foreman**  
Date: 2026-07-29 ~15:27 ET  
Command context: Ben issued `VPGM` — first V under the foreman lane.

## Vision

Every primary CTA on the site funnels to `/bellows/intake`. That room has
never had its own stranger's-eyes sweep — and first impressions on phones
are also set by weight: at least one public image still ships at 2.2 MB.

Two ideas:

1. **Funnel room sweep** — `/bellows/intake` copy, form labels, statuses,
   and empty/closed states read like an invitation, not an ops console.
   Intake stays closed; this is about how the closed room reads.
2. **Page-weight audit** — find every image >1 MB referenced by public
   routes and compress in place (same treatment as the industry-breadth
   set: ~0.6–0.9 MB or better). No visual downgrade at display size.

## Hard edges

Intake stays closed. No env/secrets, no push/deploy, no gates. Fixes land in
the polish v2 slice; re-seal. Two-key control: seal only, Heimerdinker
pushes on Ben's phrase.

## Exit

Receipt + re-sealed slice.
