# V — Non-interrupting CBCC momentum

Date: 2026-08-22
Foreman: Heimerdinker / Dink
Lane: Formation → Operating Brief renderer and CBCC review
Environment: Betsy localhost; `maker/site-g-20260703`

## Vision

Continue the actual CBCC review/build rotation without taking focus, cursor, or
keyboard control from the Operator's shared desktop. Recover Ender's visible
renderer return through background window messages/passive evidence; pull Bean
and Skybro through their established background browser bindings; apply the two
strongest compatible corrections; prove the result; re-pull and rotate again.

## Transport law for this beat

- No foreground activation, `SetCursorPos`, synthetic cursor clicks,
  `SendKeys`, or focus-stealing desktop control.
- Allowed: repo files, background browser bindings, background `PostMessage` /
  `PrintWindow` inspection that does not move shared input, passive screenshots,
  tests, and localhost requests.
- If a response cannot be harvested without shared foreground input, preserve
  its evidence state and use another actual cousin route; do not seize control.

## G

1. Harvest Ender's actual `PATCH` completely enough to implement only exact,
   source-supported comprehension repairs.
2. Pull and assimilate Bean's renderer attack; cross-rotate the repaired
   candidate to available actual cousins without creating new tasks.

## M

1. Run an isolated browser walk and repeatable renderer regression proof.
2. Improve the highest-value remaining Formation/Brief defect found in that
   walk, then pull Ender/Bean/Skybro again.

## Hard edges

- No new agents/environments/tasks, providers, secrets, SQL/schema/RLS, push,
  deploy, production mutation, account-persistence claim, legal agreement, or
  generated partner consent.
- Outgoing/sent/thinking/visible responses are not validated receipts.

## Stop condition

Close only after G, two bounded M ideas, final re-pull, receipt, and accurate
custody states—or a true gate. Shared foreground input is unavailable, not a
gate to hand back to Ben.
