export const localAuthPreviewTruth = Object.freeze({
  banner:
    "Local browser walkthrough — no Werkles account, Supabase session, or Stripe action is created here.",
  login: "Browser walkthrough only — this is not a saved account.",
  signup: "This starts the same browser-only walkthrough; no Werkles account is created."
});
