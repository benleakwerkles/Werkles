# INTENT_TO_ANATOMY_COMPILER_V0_RECEIPT

Generated: 2026-06-24

Destination: TinkerDen Intake, Speaker

## Input

Operator Intent:

> Find the hidden threads between these five disparate research papers.

## Anatomy Proposal JSON

JSON artifact:

`C:\Users\BenLeak\Documents\Codex\2026-06-20\to-swanson-doss-mission-branch-truth\outputs\INTENT_TO_ANATOMY_COMPILER_V0.json`

Decision:

`CONDITIONAL_GO`

Overall risk:

`MEDIUM`

## Natural-Language Explanation

The smallest required anatomy is:

1. `sensory membrane`
   - Needed to identify exactly which five papers are in scope.
   - Prevents wrong corpus, missing paper, duplicate paper, stale file, invented source, or analysis before source boundaries are proven.

2. `breathing`
   - Needed to prove intake, process, and exhale.
   - Prevents false delivery, false intake, and output that cannot be tied back to the five source papers.

3. `muscle`
   - Needed to perform the comparison and write a durable hidden-thread artifact.
   - Prevents intent-only analysis, no artifact, no work log, or no packet-work-artifact chain.

4. `immune`
   - Needed because "hidden threads" can easily become attractive but unsupported pattern-making.
   - Prevents hallucinated connections, overfit themes, false equivalence, and unsupported synthesis.

This does not need the whole organism. It does not need a crawler, dashboard, memory system, metabolism loop, doctrine assimilation, or ongoing watcher.

## Rejected Organs List

Rejected for V0:

- `nerves`: no watcher/event detection is required for a one-time provided corpus.
- `circulation`: no multi-machine or multi-surface routing is required for local synthesis.
- `metabolism`: no future behavior change from a prior receipt is requested.
- `assimilation`: no doctrine, behavior, or inheritance promotion is requested.
- `attraction`: candidate relationship discovery can live inside the muscle comparison step for V0.
- `endocrine`: no global priority, mood, timing, or system-wide modulation is required.
- `morphogenesis`: no new structure, UI, workflow, or organism shape is required.
- `memory consolidation`: no long-term memory promotion is required.
- `wormeyes`: the intent names five papers, not open-ended web/corpus crawling.

## Human Gates Required

No Human Gate is required for local read-only synthesis over five already-provided papers.

Human Gate is required if:

- The papers require credentials, paid access, private storage, or contain sensitive material.
- The synthesis will be sent, published, committed, or shared outside the local workspace.
- The synthesis will be promoted into doctrine, manuscript, memory, protocol, or canonical truth.

## Termination Condition

Natural death:

Stop after either:

1. `BLOCKED_SOURCE_MANIFEST` receipt is written, or
2. one hidden-thread synthesis artifact and one proof receipt are written and read back.

No watcher, crawler, dashboard, or recurring process remains alive after the proof receipt.

## First Executable Proof Step

`SOURCE_MANIFEST_RECEIPT`

Action:

Create a read-only manifest for the five papers before analysis.

Success condition:

Exactly five readable source entries exist with:

- title
- path_or_url
- hash_or_stable_id
- extraction_status
- extraction_method
- timestamp

Reject condition:

- fewer than five readable papers
- unresolvable sources
- duplicate sources
- missing hash/stable id
- no extraction path

Output artifact:

`source_manifest_receipt.json`

## PASS / FAIL

PASS.

The compiler output is restrained:

- It proposes only four required organs.
- Every proposed organ names the failure it prevents.
- It rejects non-required organs.
- It includes Human Gates.
- It includes a natural death condition.
- It includes a first executable proof step.

FAIL avoided:

- Did not propose everything.
- Did not produce vague organs.
- Did not omit death condition.
- Did not omit proof step.
