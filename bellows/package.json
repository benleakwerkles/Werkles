{
  "name": "@werkles/bellows",
  "version": "0.1.0",
  "private": true,
  "description": "Local asset-delivery infrastructure for the Werkles image pipeline",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "health": "node src/index.js health",
    "dry-run": "node scripts/dry-run.js",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/health.js && node --check src/jobs/types.js && node --check src/jobs/queue.js && node --check src/storage/local.js && node --check src/storage/supabase.js && node --check src/forge/client.js && node --check scripts/dry-run.js"
  }
}
