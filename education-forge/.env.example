# Education Forge — local env (no secrets in repo)

MAX_DRAFTS_PER_RUN=1
MAX_SOURCES_PER_DRAFT=8
DAILY_COST_LIMIT_USD=5
EDUCATION_FORGE_DRY_RUN=true

# Provider placeholders — do not commit real keys
EDUCATION_FORGE_PROVIDER=
EDUCATION_FORGE_API_KEY=
